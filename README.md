## Build Setup

``` bash
# clone repository
$ git clone https://github.com/jacobthomsen/csgoultimategg.git

# install dependencies
$ npm install # Or yarn install

# serve with hot reload at localhost:3000
$ npm run dev

# build for production and launch server
$ npm start

